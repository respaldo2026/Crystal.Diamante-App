# 🔧 SNIPPETS LISTOS PARA COPIAR Y PEGAR\n\n**Ubicación:** Código listo para integrar en tu proyecto\n\n---\n\n## 1️⃣ Integración en Página de Inscripción\n\n**Ubicación:** `src/components/FormularioInscripcion.tsx` (o similar)\n\n```typescript\n'use client';\n\nimport React, { useState } from 'react';\nimport { enviarConfirmacionInscripcion } from '@/services/whatsapp-messages-module';\nimport { supabaseBrowserClient } from '@/utils/supabase/client';\n\ninterface FormInscripcionProps {\n  cursoId: string;\n  cursoData: {\n    nombre: string;\n    fechaInicio: string;\n    horario: string;\n    costo: number;\n    instructor: string;\n  };\n}\n\nexport function FormularioInscripcion({ cursoId, cursoData }: FormInscripcionProps) {\n  const [cargando, setCargando] = useState(false);\n  const [mensaje, setMensaje] = useState('');\n\n  const handleInscribir = async (e: React.FormEvent<HTMLFormElement>) => {\n    e.preventDefault();\n    setCargando(true);\n    setMensaje('');\n\n    try {\n      const formData = new FormData(e.currentTarget);\n      const nombre = formData.get('nombre') as string;\n      const email = formData.get('email') as string;\n      const telefono = formData.get('telefono') as string;\n      const cedula = formData.get('cedula') as string;\n\n      // 1. Crear inscripción en BD\n      const { data: inscripcion, error } = await supabaseBrowserClient\n        .from('inscripciones')\n        .insert([\n          {\n            curso_id: cursoId,\n            usuario_id: 'obtener-del-user',  // Usar user actual\n            estado: 'confirmada',\n            fecha_inscripcion: new Date().toISOString(),\n            // ... otros campos\n          },\n        ])\n        .select()\n        .single();\n\n      if (error) throw error;\n\n      // 2. Calcular fecha de próximo pago (5 días)\n      const fechaPago = new Date();\n      fechaPago.setDate(fechaPago.getDate() + 5);\n\n      // 3. Enviar confirmación por WhatsApp\n      const resultadoWhatsApp = await enviarConfirmacionInscripcion(\n        'user-id-actual',  // Usar user.id actual\n        {\n          nombre,\n          telefono,\n          nombreCurso: cursoData.nombre,\n          fechaInicio: cursoData.fechaInicio,\n          horario: cursoData.horario,\n          mensualidad: cursoData.costo,\n          instructor: cursoData.instructor,\n          fechaPago: fechaPago.toLocaleDateString('es-CO'),\n        }\n      );\n\n      // 4. Mostrar mensaje\n      if (resultadoWhatsApp.exito) {\n        setMensaje('✅ ¡Inscripción confirmada! Revisa tu WhatsApp para más detalles.');\n      } else {\n        setMensaje('✅ Inscripción creada, pero WhatsApp fallo (se enviará después)');\n      }\n\n      // 5. Limpiar y redirigir\n      setTimeout(() => {\n        window.location.href = '/dashboard/mis-cursos';\n      }, 2000);\n    } catch (error) {\n      console.error('Error:', error);\n      setMensaje('❌ Error al inscribirse. Intenta de nuevo.');\n    } finally {\n      setCargando(false);\n    }\n  };\n\n  return (\n    <form onSubmit={handleInscribir} className=\"form-inscripcion\">\n      <input name=\"nombre\" type=\"text\" placeholder=\"Tu nombre\" required />\n      <input name=\"email\" type=\"email\" placeholder=\"Tu email\" required />\n      <input name=\"telefono\" type=\"tel\" placeholder=\"573001234567\" required />\n      <input name=\"cedula\" type=\"text\" placeholder=\"Tu cédula\" />\n      \n      <button type=\"submit\" disabled={cargando}>\n        {cargando ? 'Procesando...' : 'Inscribirme'}\n      </button>\n      \n      {mensaje && <p className=\"mensaje\">{mensaje}</p>}\n    </form>\n  );\n}\n```\n\n---\n\n## 2️⃣ Integración en Procesamiento de Pago\n\n**Ubicación:** `src/app/api/pagos/confirmar/route.ts` (o similar)\n\n```typescript\nimport { NextRequest, NextResponse } from 'next/server';\nimport { enviarConfirmacionPago } from '@/services/whatsapp-messages-module';\nimport { supabaseBrowserClient } from '@/utils/supabase/client';\n\nexport async function POST(request: NextRequest) {\n  try {\n    const body = await request.json();\n    const { pagoId, usuarioId, monto, referencia } = body;\n\n    // 1. Obtener datos del usuario\n    const { data: usuario } = await supabaseBrowserClient\n      .from('usuarios')\n      .select('nombre, telefono, inscripciones:inscripciones(cursos:curso_id(nombre))')\n      .eq('id', usuarioId)\n      .single();\n\n    if (!usuario?.telefono) {\n      throw new Error('Usuario sin teléfono registrado');\n    }\n\n    // 2. Obtener datos del pago\n    const { data: pago } = await supabaseBrowserClient\n      .from('pagos')\n      .select('*')\n      .eq('id', pagoId)\n      .single();\n\n    // 3. Actualizar estado del pago\n    const { error: updateError } = await supabaseBrowserClient\n      .from('pagos')\n      .update({ estado: 'pagado', fecha_pago: new Date().toISOString() })\n      .eq('id', pagoId);\n\n    if (updateError) throw updateError;\n\n    // 4. Calcular fecha de vigencia\n    const fechaVigencia = new Date();\n    fechaVigencia.setDate(fechaVigencia.getDate() + 30);\n\n    // 5. Enviar confirmación por WhatsApp\n    const inscripcion = usuario.inscripciones?.[0];\n    const proximaClase = new Date();\n    proximaClase.setDate(proximaClase.getDate() + 3);\n\n    const resultado = await enviarConfirmacionPago(\n      usuarioId,\n      {\n        nombre: usuario.nombre,\n        telefono: usuario.telefono,\n        referenciaPago: referencia || pagoId.substring(0, 10),\n        monto,\n        fechaPago: new Date().toLocaleDateString('es-CO'),\n        concepto: pago?.concepto || 'Pago de matrícula',\n        nombreCurso: inscripcion?.cursos?.nombre || 'Tu curso',\n        fechaVigencia: fechaVigencia.toLocaleDateString('es-CO'),\n        fechaProximaClase: proximaClase.toLocaleDateString('es-CO'),\n      }\n    );\n\n    // 6. Responder\n    return NextResponse.json({\n      success: true,\n      message: 'Pago confirmado',\n      whatsappEnviado: resultado.exito,\n    });\n  } catch (error) {\n    console.error('Error en confirmación de pago:', error);\n    return NextResponse.json(\n      { error: 'Error procesando pago' },\n      { status: 500 }\n    );\n  }\n}\n```\n\n---\n\n## 3️⃣ Botón en Página de Curso\n\n**Ubicación:** `src/components/DetallleCurso.tsx`\n\n```typescript\n'use client';\n\nimport React, { useState } from 'react';\nimport { enviarInformacionCurso } from '@/services/whatsapp-messages-module';\nimport { useSession } from '@/providers/auth-provider/auth-provider.client';\n\ninterface CursoDetalleProps {\n  cursoId: string;\n  curso: {\n    nombre: string;\n    descripcion: string;\n    duracion: string;\n    horario: string;\n    modalidad: 'presencial' | 'virtual' | 'hibrida';\n    requisitos: string;\n    costoInscripcion: number;\n    costoMensualidad: number;\n    duracionMeses: number;\n    cuposDisponibles: number;\n    queIncluye: string;\n    fechaProximaCohorte: string;\n    fechaCierreInscripcion: string;\n  };\n}\n\nexport function DetalleCurso({ cursoId, curso }: CursoDetalleProps) {\n  const { user } = useSession();\n  const [enviandoWhatsApp, setEnviandoWhatsApp] = useState(false);\n  const [mensajeWhatsApp, setMensajeWhatsApp] = useState('');\n\n  const handleEnviarPorWhatsApp = async () => {\n    if (!user?.phone_number) {\n      setMensajeWhatsApp('⚠️ Por favor, actualiza tu teléfono en el perfil');\n      return;\n    }\n\n    try {\n      setEnviandoWhatsApp(true);\n      setMensajeWhatsApp('Enviando...');\n\n      const resultado = await enviarInformacionCurso(\n        user.phone_number,\n        {\n          nombre: user.user_metadata?.nombre || 'Estudiante',\n          nombreCurso: curso.nombre,\n          descripcionCurso: curso.descripcion,\n          duracion: curso.duracion,\n          horario: curso.horario,\n          modalidad: curso.modalidad,\n          requisitos: curso.requisitos,\n          costoInscripcion: curso.costoInscripcion,\n          costoMensualidad: curso.costoMensualidad,\n          duracionMeses: curso.duracionMeses,\n          cuposDisponibles: curso.cuposDisponibles,\n          queIncluye: curso.queIncluye,\n          fechaProximaCohorte: curso.fechaProximaCohorte,\n          fechaCierreInscripcion: curso.fechaCierreInscripcion,\n          linkInscripcion: `${process.env.NEXT_PUBLIC_APP_URL}/cursos/${cursoId}/inscribir`,\n          usuarioId: user.id,\n        }\n      );\n\n      if (resultado.exito) {\n        setMensajeWhatsApp('✅ Información enviada a tu WhatsApp!');\n      } else {\n        setMensajeWhatsApp(`❌ Error: ${resultado.error || 'Intenta de nuevo'}`);\n      }\n    } catch (error) {\n      setMensajeWhatsApp('❌ Error al enviar información');\n      console.error(error);\n    } finally {\n      setEnviandoWhatsApp(false);\n    }\n  };\n\n  return (\n    <div className=\"curso-detalle\">\n      <h1>{curso.nombre}</h1>\n      <p>{curso.descripcion}</p>\n\n      <div className=\"info-curso\">\n        <p>📚 Duración: {curso.duracion}</p>\n        <p>🕐 Horario: {curso.horario}</p>\n        <p>💰 Mensualidad: ${curso.costoMensualidad.toLocaleString()}</p>\n        <p>👥 Cupos: {curso.cuposDisponibles}</p>\n      </div>\n\n      <div className=\"botones\">\n        <button \n          className=\"btn-primario\"\n          onClick={() => window.location.href = `/cursos/${cursoId}/inscribir`}\n        >\n          Inscribirse\n        </button>\n\n        <button\n          className=\"btn-secundario\"\n          onClick={handleEnviarPorWhatsApp}\n          disabled={enviandoWhatsApp}\n        >\n          📱 {enviandoWhatsApp ? 'Enviando...' : 'Enviarme por WhatsApp'}\n        </button>\n      </div>\n\n      {mensajeWhatsApp && (\n        <small className=\"mensaje-whatsapp\">{mensajeWhatsApp}</small>\n      )}\n    </div>\n  );\n}\n```\n\n---\n\n## 4️⃣ Cron Job: Recordatorio de Pago\n\n**Ubicación:** `src/app/api/cron/recordatorios-pago/route.ts`\n\n```typescript\nimport { NextRequest, NextResponse } from 'next/server';\nimport { enviarRecordatorPago } from '@/services/whatsapp-messages-module';\nimport { createServerClient } from '@supabase/ssr';\nimport { cookies } from 'next/headers';\n\nexport async function GET(request: NextRequest) {\n  // Verificar que es una llamada válida de cron\n  const authHeader = request.headers.get('authorization');\n  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {\n    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  try {\n    const cookieStore = await cookies();\n    const supabase = createServerClient(\n      process.env.NEXT_PUBLIC_SUPABASE_URL!,\n      process.env.SUPABASE_SERVICE_ROLE_KEY!,\n      {\n        cookies: {\n          get: (name) => cookieStore.get(name)?.value,\n          set: (name, value, options) => cookieStore.set(name, value, options),\n          remove: (name, options) => cookieStore.delete(name),\n        },\n      }\n    );\n\n    // Calcular rango de fechas (3-5 días)\n    const hoy = new Date();\n    const en3Dias = new Date(hoy.getTime() + 3 * 24 * 60 * 60 * 1000);\n    const en5Dias = new Date(hoy.getTime() + 5 * 24 * 60 * 60 * 1000);\n\n    // Obtener pagos pendientes\n    const { data: pagos, error } = await supabase\n      .from('pagos')\n      .select(`\n        id,\n        usuario_id,\n        monto,\n        mes,\n        fecha_vencimiento,\n        usuarios:usuario_id (nombre, telefono),\n        cursos:curso_id (nombre)\n      `)\n      .eq('estado', 'pendiente')\n      .gte('fecha_vencimiento', en3Dias.toISOString())\n      .lt('fecha_vencimiento', en5Dias.toISOString());\n\n    if (error) throw error;\n\n    if (!pagos || pagos.length === 0) {\n      return NextResponse.json({\n        success: true,\n        procesados: 0,\n        mensaje: 'No hay pagos próximos a recordar',\n      });\n    }\n\n    let exitosos = 0;\n    let fallidos = 0;\n\n    // Enviar recordatorio a cada usuario\n    for (const pago of pagos) {\n      const usuario = (pago.usuarios as any)?.[0] || {};\n      const curso = (pago.cursos as any)?.[0] || {};\n\n      const resultado = await enviarRecordatorPago(pago.usuario_id, {\n        nombre: usuario.nombre || 'Estudiante',\n        telefono: usuario.telefono || '',\n        mes: pago.mes || 'Febrero 2026',\n        monto: pago.monto || 0,\n        fechaVencimiento: new Date(pago.fecha_vencimiento).toLocaleDateString('es-CO'),\n        nombreCurso: curso.nombre || 'Tu curso',\n      });\n\n      resultado.exito ? exitosos++ : fallidos++;\n    }\n\n    return NextResponse.json({\n      success: true,\n      procesados: pagos.length,\n      exitosos,\n      fallidos,\n    });\n  } catch (error) {\n    console.error('[Cron] Error recordatorios:', error);\n    return NextResponse.json(\n      { error: 'Error procesando recordatorios' },\n      { status: 500 }\n    );\n  }\n}\n```\n\n---\n\n## 5️⃣ Cron Job: Recordatorio de Clase\n\n**Ubicación:** `src/app/api/cron/recordatorios-clase/route.ts`\n\n```typescript\nimport { NextRequest, NextResponse } from 'next/server';\nimport { enviarRecordatorioClase } from '@/services/whatsapp-messages-module';\nimport { createServerClient } from '@supabase/ssr';\nimport { cookies } from 'next/headers';\n\nexport async function GET(request: NextRequest) {\n  const authHeader = request.headers.get('authorization');\n  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {\n    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n\n  try {\n    const cookieStore = await cookies();\n    const supabase = createServerClient(\n      process.env.NEXT_PUBLIC_SUPABASE_URL!,\n      process.env.SUPABASE_SERVICE_ROLE_KEY!,\n      {\n        cookies: {\n          get: (name) => cookieStore.get(name)?.value,\n          set: (name, value, options) => cookieStore.set(name, value, options),\n          remove: (name, options) => cookieStore.delete(name),\n        },\n      }\n    );\n\n    // Buscar clases que comienzan en ~1 hora\n    const ahora = new Date();\n    const en1Hora = new Date(ahora.getTime() + 60 * 60 * 1000);\n    const hace30Min = new Date(ahora.getTime() - 30 * 60 * 1000);\n\n    const { data: clases, error } = await supabase\n      .from('clases')\n      .select(`\n        id,\n        nombre,\n        fecha_hora,\n        ubicacion,\n        instructor_id,\n        cursos:curso_id (nombre),\n        clase_usuarios:usuario_id (*)\n      `)\n      .gte('fecha_hora', hace30Min.toISOString())\n      .lt('fecha_hora', en1Hora.toISOString());\n\n    if (error) throw error;\n\n    let totalEnviados = 0;\n\n    for (const clase of clases || []) {\n      const horaClase = new Date(clase.fecha_hora).toLocaleTimeString('es-CO');\n\n      // Obtener instructor\n      const { data: instructor } = await supabase\n        .from('usuarios')\n        .select('nombre')\n        .eq('id', clase.instructor_id)\n        .single();\n\n      // Enviar a cada estudiante\n      const estudiantes = (clase.clase_usuarios as any) || [];\n      for (const estudiante of estudiantes) {\n        const resultado = await enviarRecordatorioClase(estudiante.id, {\n          nombre: estudiante.nombre,\n          telefono: estudiante.telefono,\n          nombreCurso: (clase.cursos as any)?.nombre || 'Tu curso',\n          horaClase,\n          ubicacion: clase.ubicacion || 'Por definir',\n          nombreInstructor: instructor?.nombre || 'Tu instructor',\n        });\n\n        if (resultado.exito) totalEnviados++;\n      }\n    }\n\n    return NextResponse.json({\n      success: true,\n      clasesEncontradas: (clases || []).length,\n      recordatoriosEnviados: totalEnviados,\n    });\n  } catch (error) {\n    console.error('[Cron] Error recordatorios clase:', error);\n    return NextResponse.json(\n      { error: 'Error procesando recordatorios' },\n      { status: 500 }\n    );\n  }\n}\n```\n\n---\n\n## 6️⃣ Hook de React: Usar WhatsApp en Componentes\n\n**Ubicación:** `src/hooks/useWhatsApp.ts`\n\n```typescript\n'use client';\n\nimport { useState, useCallback } from 'react';\nimport {\n  enviarConfirmacionInscripcion,\n  enviarConfirmacionPago,\n  enviarInformacionCurso,\n  // ... importar otros según necesites\n} from '@/services/whatsapp-messages-module';\n\ninterface UseWhatsAppReturn {\n  enviando: boolean;\n  error: string | null;\n  exito: boolean;\n  enviarConfirmacionInscripcion: (datos: any) => Promise<void>;\n  enviarConfirmacionPago: (datos: any) => Promise<void>;\n  enviarInformacionCurso: (datos: any) => Promise<void>;\n  resetear: () => void;\n}\n\nexport function useWhatsApp(): UseWhatsAppReturn {\n  const [enviando, setEnviando] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n  const [exito, setExito] = useState(false);\n\n  const resetear = useCallback(() => {\n    setError(null);\n    setExito(false);\n  }, []);\n\n  const handleEnviarInscripcion = useCallback(async (datos: any) => {\n    try {\n      setEnviando(true);\n      setError(null);\n\n      const resultado = await enviarConfirmacionInscripcion(\n        datos.usuarioId,\n        datos\n      );\n\n      if (resultado.exito) {\n        setExito(true);\n      } else {\n        setError(resultado.error || 'Error desconocido');\n      }\n    } catch (err) {\n      setError(err instanceof Error ? err.message : 'Error desconocido');\n    } finally {\n      setEnviando(false);\n    }\n  }, []);\n\n  // Similar para otros métodos...\n\n  return {\n    enviando,\n    error,\n    exito,\n    enviarConfirmacionInscripcion: handleEnviarInscripcion,\n    enviarConfirmacionPago: async () => {},\n    enviarInformacionCurso: async () => {},\n    resetear,\n  };\n}\n\n// Uso en componente:\n// const { enviando, error, exito, enviarConfirmacionInscripcion } = useWhatsApp();\n```\n\n---\n\n## 7️⃣ Validación de Teléfono\n\n**Ubicación:** `src/utils/validaciones.ts`\n\n```typescript\n/**\n * Valida que un número de teléfono sea válido para Colombia\n */\nexport function validarTelefonoColombia(telefono: string): boolean {\n  // Remover espacios y caracteres especiales\n  const limpio = telefono.replace(/\\D/g, '');\n\n  // Debe empezar con 57 (código Colombia) y tener 12 dígitos total\n  // O tener 10 dígitos si no incluye código de país\n  if (limpio.startsWith('57') && limpio.length === 12) {\n    return true;\n  }\n\n  // O simplemente 10 dígitos\n  if (limpio.length === 10 && /^3\\d{9}$/.test(limpio)) {\n    return true;\n  }\n\n  return false;\n}\n\n/**\n * Formatea el teléfono a formato internacional\n */\nexport function formatearTelefonoInternacional(telefono: string): string {\n  const limpio = telefono.replace(/\\D/g, '');\n  const conCodigo = limpio.startsWith('57') ? limpio : '57' + limpio;\n  return conCodigo;\n}\n\n// Uso:\n// const es_valido = validarTelefonoColombia('573001234567');\n// const formateado = formatearTelefonoInternacional('3001234567'); // '573001234567'\n```\n\n---\n\n## 8️⃣ Test: Script para Probar Manualmente\n\n**Ubicación:** `src/scripts/test-whatsapp.ts`\n\n```typescript\n/**\n * Script para testear WhatsApp manualmente\n * Ejecutar: npx ts-node src/scripts/test-whatsapp.ts\n */\n\nimport { enviarConfirmacionInscripcion } from '@/services/whatsapp-messages-module';\n\nconst testConfirmacionInscripcion = async () => {\n  console.log('🔄 Testando confirmación de inscripción...');\n\n  const resultado = await enviarConfirmacionInscripcion(\n    'test-user-id',\n    {\n      nombre: 'Test User',\n      telefono: '573001234567', // CAMBIAR A TU NÚMERO\n      nombreCurso: 'Python Avanzado',\n      fechaInicio: '2026-02-15',\n      horario: '2:00 PM',\n      mensualidad: 250000,\n      instructor: 'Profesor Test',\n      fechaPago: '2026-02-10',\n    }\n  );\n\n  console.log('✅ Resultado:', resultado);\n\n  if (resultado.exito) {\n    console.log('✅ Mensaje enviado exitosamente');\n    console.log('📱 MessageID:', resultado.mensajeId);\n  } else {\n    console.log('❌ Error:', resultado.error);\n  }\n};\n\n// Ejecutar\ntestConfirmacionInscripcion().catch(console.error);\n```\n\n---\n\n## ✨ Resumen de Archivos a Crear\n\n```\n1. src/services/whatsapp-messages-module.ts  ← Copiar FASE-2-3\n2. src/components/FormularioInscripcion.tsx  ← Snippet 1️⃣\n3. src/app/api/pagos/confirmar/route.ts      ← Snippet 2️⃣\n4. src/components/DetalleCurso.tsx           ← Snippet 3️⃣\n5. src/app/api/cron/recordatorios-pago/route.ts      ← Snippet 4️⃣\n6. src/app/api/cron/recordatorios-clase/route.ts     ← Snippet 5️⃣\n7. src/hooks/useWhatsApp.ts                  ← Snippet 6️⃣\n8. src/utils/validaciones.ts                 ← Snippet 7️⃣\n9. src/scripts/test-whatsapp.ts              ← Snippet 8️⃣\n```\n\n---\n\n**¡Todos los snippets están listos para copiar y pegar! 🚀**\n"